{
  "name": "rolling-image-link-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Fetch featured Pexels image links for the rolling image strip.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/main.js"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
